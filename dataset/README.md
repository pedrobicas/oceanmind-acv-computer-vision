# Dataset

Gere o dataset sintético com:

```bash
python src/generate_synthetic_ocean_dataset.py --output dataset --images-per-class 220 --size 96
```

As subpastas das classes serão criadas automaticamente.
